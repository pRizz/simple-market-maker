---
phase: 01-provider-settings-and-secrets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/settings/domain/app-settings.ts
  - src/modules/settings/domain/provider-registry.ts
  - src/modules/settings/domain/provider-api-key.ts
  - src/modules/db/schema.ts
  - src/modules/db/migrations/0002_provider_settings_and_secrets.sql
  - src/modules/db/migrations/meta/_journal.json
  - test/settings/app-settings.test.ts
  - test/settings/provider-registry.test.ts
autonomous: true
requirements:
  - SET-02
  - SET-03
  - SET-04
  - KEY-01
  - KEY-02
  - KEY-03
generated_by: gsd-plan-phase
lifecycle_mode: interactive
phase_lifecycle_id: 01-2026-04-26T16-55-02
generated_at: 2026-04-26T17:48:48Z
must_haves:
  truths:
    - "Settings have durable defaults for alpha_vantage, confirm_before_fetch, and hidden sample data."
    - "Settings parser accepts only selectable runnable real default providers; sample and twelve_data are rejected as defaultProvider values."
    - "Provider descriptors expose safe metadata for alpha_vantage, sample, and twelve_data without secret values."
    - "Provider API key storage has server-only encrypted material columns plus safe metadata columns."
  artifacts:
    - path: "src/modules/settings/domain/app-settings.ts"
      provides: "App settings domain types, defaults, and boundary parser"
    - path: "src/modules/settings/domain/provider-registry.ts"
      provides: "Provider descriptor registry and capability helpers for alpha_vantage, sample, and twelve_data"
    - path: "src/modules/settings/domain/provider-api-key.ts"
      provides: "Safe provider key metadata and validation status types"
    - path: "src/modules/db/schema.ts"
      provides: "app_settings and provider_api_keys Drizzle table definitions"
    - path: "src/modules/db/migrations/0002_provider_settings_and_secrets.sql"
      provides: "Generated SQL migration for settings and provider key tables"
  key_links:
    - from: "src/modules/settings/domain/app-settings.ts"
      to: "src/modules/db/schema.ts"
      via: "matching default_provider, missing_data_behavior, and show_sample_data values"
      pattern: "alpha_vantage|confirm_before_fetch|show_sample_data"
    - from: "src/modules/settings/domain/app-settings.ts"
      to: "src/modules/settings/domain/provider-registry.ts"
      via: "defaultProvider validation uses selectable runnable real provider ids only"
      pattern: "selectableDefaultProviderIds|isSelectableDefaultProviderId"
    - from: "src/modules/settings/domain/provider-api-key.ts"
      to: "src/modules/db/schema.ts"
      via: "safe metadata fields mapped separately from encrypted columns"
      pattern: "masked_suffix|validation_status|encrypted_key|encryption_iv|encryption_auth_tag"
---

<objective>
Create the Phase 1 settings and provider-key contracts, database schema, and generated migration.

Purpose: Downstream plans need stable domain types and persisted tables before implementing server services, provider resolution, and UI.
Output: Settings domain modules, provider registry, provider-key metadata types, Drizzle schema updates, and a reviewed migration.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-provider-settings-and-secrets/01-CONTEXT.md
@.planning/phases/01-provider-settings-and-secrets/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@src/modules/db/schema.ts
@src/modules/market-data/domain/market-data-chunk.ts
@src/modules/market-data/domain/market-data-validation.ts

<interfaces>
Existing market-data source contract:
```typescript
export const marketDataSources = ["sample", "alpha_vantage"] as const;
export const marketDataIntervals = ["daily"] as const;
export type MarketDataSource = (typeof marketDataSources)[number];
export type MarketDataInterval = (typeof marketDataIntervals)[number];
```

New settings contract to create:
```typescript
export const providerIds = ["alpha_vantage", "sample", "twelve_data"] as const;
export const selectableDefaultProviderIds = ["alpha_vantage"] as const;
export const missingDataBehaviors = ["confirm_before_fetch", "silent_fetch"] as const;
export type ProviderId = (typeof providerIds)[number];
export type DefaultProviderId = (typeof selectableDefaultProviderIds)[number];
export type MissingDataBehavior = (typeof missingDataBehaviors)[number];
export type AppSettings = {
  defaultProvider: DefaultProviderId;
  missingDataBehavior: MissingDataBehavior;
  showSampleData: boolean;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create settings and provider registry domain contracts</name>
  <files>src/modules/settings/domain/app-settings.ts, src/modules/settings/domain/provider-registry.ts, src/modules/settings/domain/provider-api-key.ts, test/settings/app-settings.test.ts, test/settings/provider-registry.test.ts</files>
  <read_first>
    - .planning/phases/01-provider-settings-and-secrets/01-CONTEXT.md
    - .planning/phases/01-provider-settings-and-secrets/01-RESEARCH.md
    - src/modules/market-data/domain/market-data-chunk.ts
    - src/modules/market-data/domain/market-data-validation.ts
    - test/market-data/market-data-validation.test.ts
  </read_first>
  <behavior>
    - Test default settings: `defaultAppSettings.defaultProvider` is `alpha_vantage`, `missingDataBehavior` is `confirm_before_fetch`, and `showSampleData` is `false` per D-03 and D-04.
    - Test parser behavior: `maybeParseAppSettingsInput` accepts `defaultProvider: "alpha_vantage"` and rejects `defaultProvider: "sample"` and `defaultProvider: "twelve_data"` with a `defaultProvider` field error, because Phase 1 default providers are limited to implemented runnable real providers per D-04, D-08, D-09, and D-10.
    - Test parser behavior: `maybeParseAppSettingsInput` accepts missing-data behavior values `confirm_before_fetch` or `silent_fetch`, and a boolean `showSampleData`.
    - Test registry behavior: `alpha_vantage` descriptor is `implemented`, requires a key, is selectable as the default provider, is key-manageable, has `maybeEnvironmentFallbackName: "ALPHA_VANTAGE_API_KEY"`, and supports `daily` per D-06 through D-08.
    - Test sample descriptor: `sample` is `demo`, has no key requirement, supports `daily`, is not selectable as `defaultProvider`, is not key-manageable, and safe text says it is gated demo/development data per D-09.
    - Test Twelve Data descriptor: `twelve_data` is `planned`, key requirement is `planned`, is not selectable as `defaultProvider`, is not key-manageable, and no adapter behavior is added per D-10.
  </behavior>
  <action>
    Create `src/modules/settings/domain/app-settings.ts` with `providerIds`, `ProviderId`, `selectableDefaultProviderIds`, `DefaultProviderId`, `missingDataBehaviors`, `MissingDataBehavior`, `AppSettings`, `RawAppSettingsInput`, `ParsedAppSettingsResult`, `defaultAppSettings`, `isSelectableDefaultProviderId`, and `maybeParseAppSettingsInput`. Use Zod at the boundary and return `{ ok: true, value } | { ok: false, fieldErrors, formErrors }` following the existing `maybeParseMarketDataChunk` style. The `defaultProvider` parser must use `selectableDefaultProviderIds`, not all `providerIds`; Phase 1 must reject `sample` because it is controlled only by `showSampleData`, and must reject `twelve_data` until the Phase 4 adapter exists.

    Create `src/modules/settings/domain/provider-registry.ts` with `ProviderDescriptor` and `providerDescriptors` keyed by exact ids `alpha_vantage`, `sample`, and `twelve_data`. Include descriptor fields `id`, `label`, `implementationStatus`, `keyRequirement`, `maybeEnvironmentFallbackName`, `supportedIntervals`, `safeDescription`, `isSelectableDefault`, and `isKeyManageable` per D-06 and D-07. Export `keyManageableProviderIds` and `isKeyManageableProviderId`; in Phase 1 the only key-manageable provider is `alpha_vantage`. Do not add a Twelve Data adapter or behavior.

    Create `src/modules/settings/domain/provider-api-key.ts` with `providerKeyValidationStatuses`, `ProviderKeyValidationStatus`, `ProviderApiKeyMetadata`, `RawProviderApiKeyInput`, and helper `maskedSuffixFromApiKey`. Keep encrypted row shapes out of this shared domain file; Plan 02 should define encrypted repository row types inside server-only modules. `ProviderApiKeyMetadata` must include only `providerId`, `providerLabel`, `enabled`, `maskedSuffix`, `validationStatus`, `validationMessage`, `lastValidatedAt`, `createdAt`, and `updatedAt`. It must not include plaintext, `encryptedKey`, `encryptionIv`, or `encryptionAuthTag` per D-13 and D-14.
  </action>
  <verify>
    <automated>bun run test -- test/settings/app-settings.test.ts test/settings/provider-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "alpha_vantage|confirm_before_fetch|silent_fetch|showSampleData|selectableDefaultProviderIds|DefaultProviderId|isSelectableDefaultProviderId" src/modules/settings/domain/app-settings.ts test/settings/app-settings.test.ts` finds the locked values and selectable default-provider contract.
    - `rg -n "ALPHA_VANTAGE_API_KEY|twelve_data|sample|isKeyManageable|keyManageableProviderIds" src/modules/settings/domain/provider-registry.ts test/settings/provider-registry.test.ts` finds descriptor and capability coverage.
    - `rg -n "sample|twelve_data" test/settings/app-settings.test.ts` finds negative tests proving both values are rejected for `defaultProvider`.
    - `rg -n "encryptedKey|encryptionIv|encryptionAuthTag" src/modules/settings/domain/provider-api-key.ts` returns no matches.
    - Any `apiKey` reference in `src/modules/settings/domain/provider-api-key.ts` is limited to raw input typing and does not appear in `ProviderApiKeyMetadata`.
  </acceptance_criteria>
  <done>Domain contracts and tests encode settings defaults, provider descriptor metadata, and safe key metadata boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add app_settings and provider_api_keys schema</name>
  <files>src/modules/db/schema.ts, test/settings/app-settings.test.ts, test/settings/provider-registry.test.ts</files>
  <read_first>
    - src/modules/db/schema.ts
    - src/modules/db/migrations/0000_initial.sql
    - src/modules/db/migrations/0001_market_data_chunks.sql
    - src/modules/settings/domain/app-settings.ts
    - src/modules/settings/domain/provider-api-key.ts
  </read_first>
  <behavior>
    - Test the domain constants used by schema defaults remain `alpha_vantage`, `confirm_before_fetch`, and `false`.
    - Test `ProviderApiKeyMetadata` remains safe metadata only after schema adds encrypted storage fields.
  </behavior>
  <action>
    Update `src/modules/db/schema.ts` to import `boolean` from `drizzle-orm/pg-core`. Add `appSettingsTable = pgTable("app_settings", ...)` with exact columns: `id` as `text("id").primaryKey().default("singleton")`, `defaultProvider` mapped to `default_provider` text not null default `alpha_vantage`, `missingDataBehavior` mapped to `missing_data_behavior` text not null default `confirm_before_fetch`, `showSampleData` mapped to `show_sample_data` boolean not null default `false`, plus `createdAt` and `updatedAt` timestamptz defaults.

    Add `providerApiKeysTable = pgTable("provider_api_keys", ...)` with exact columns: `id` uuid default random primary key, `providerId` mapped to `provider_id` text not null unique, `encryptedKey` mapped to `encrypted_key` text not null, `encryptionIv` mapped to `encryption_iv` text not null, `encryptionAuthTag` mapped to `encryption_auth_tag` text not null, `maskedSuffix` mapped to `masked_suffix` text not null, `enabled` boolean not null default true, `validationStatus` mapped to `validation_status` text not null default `not_validated`, nullable `validationMessage` mapped to `validation_message`, nullable `lastValidatedAt` timestamptz, plus `createdAt` and `updatedAt` timestamptz defaults. Use text provider ids instead of a Postgres enum so `twelve_data` descriptor metadata can exist without a provider-adapter migration.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "app_settings|provider_api_keys|default_provider|missing_data_behavior|show_sample_data" src/modules/db/schema.ts` finds the settings schema.
    - `rg -n "encrypted_key|encryption_iv|encryption_auth_tag|masked_suffix|validation_status|last_validated_at" src/modules/db/schema.ts` finds the provider key schema.
    - `rg -n "provider_id.*unique|unique.*provider_id" src/modules/db/schema.ts` shows one saved key per provider is enforced per D-12.
  </acceptance_criteria>
  <done>Drizzle schema defines singleton settings and one-row-per-provider encrypted key storage.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Generate and review Drizzle migration</name>
  <files>src/modules/db/migrations/0002_provider_settings_and_secrets.sql, src/modules/db/migrations/meta/_journal.json</files>
  <read_first>
    - src/modules/db/schema.ts
    - drizzle.config.ts
    - package.json
    - src/modules/db/migrations/meta/_journal.json
    - src/modules/db/migrations/0001_market_data_chunks.sql
  </read_first>
  <action>
    Run `bun run db:generate` after the schema edits are complete. If Drizzle creates a differently named `0002_*.sql`, rename it to `src/modules/db/migrations/0002_provider_settings_and_secrets.sql` and update `src/modules/db/migrations/meta/_journal.json` so the entry tag is `0002_provider_settings_and_secrets`. Review the generated SQL and metadata changes.

    Ensure the migration creates `app_settings` and `provider_api_keys`, includes a uniqueness constraint or index on `provider_api_keys.provider_id`, and includes a seed row:
    `INSERT INTO app_settings (id, default_provider, missing_data_behavior, show_sample_data) VALUES ('singleton', 'alpha_vantage', 'confirm_before_fetch', false) ON CONFLICT (id) DO NOTHING;`

    Do not use `npx drizzle-kit push`. Do not add any value for `PROVIDER_KEYS_ENCRYPTION_SECRET`, `ALPHA_VANTAGE_API_KEY`, provider plaintext, ciphertext examples, IV examples, or auth-tag examples to the migration or metadata.
  </action>
  <verify>
    <automated>bun run db:generate</automated>
    <automated>rg -n "CREATE TABLE .*app_settings|CREATE TABLE .*provider_api_keys|provider_id|INSERT INTO app_settings" src/modules/db/migrations/0002_provider_settings_and_secrets.sql src/modules/db/migrations/meta/_journal.json</automated>
    <automated>if [[ -n "${DATABASE_URL:-}" ]]; then bun run db:migrate; else printf '%s\n' "DATABASE_URL not configured; migration application not run."; fi</automated>
  </verify>
  <acceptance_criteria>
    - `src/modules/db/migrations/0002_provider_settings_and_secrets.sql` exists and contains `app_settings`, `provider_api_keys`, `provider_id`, and the singleton seed row.
    - `src/modules/db/migrations/meta/_journal.json` includes a new idx 2 entry with tag `0002_provider_settings_and_secrets`.
    - `rg -n "PROVIDER_KEYS_ENCRYPTION_SECRET|ALPHA_VANTAGE_API_KEY|apikey|demo-key|secret" src/modules/db/migrations/0002_provider_settings_and_secrets.sql src/modules/db/migrations/meta/_journal.json` returns no secret material beyond column names and nonsecret identifiers.
  </acceptance_criteria>
  <done>Migration artifacts are generated, reviewed, and ready to run before any downstream service relies on the new tables.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domain to database | Safe domain metadata maps to database rows that also contain encrypted secret material. |
| migration to runtime | Schema defaults and seed values become runtime behavior for missing settings rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `provider_api_keys` schema and metadata types | mitigate | Store plaintext nowhere, keep `encrypted_key`, `encryption_iv`, and `encryption_auth_tag` only in server row types, and keep `ProviderApiKeyMetadata` free of encrypted and plaintext fields. |
| T-01-02 | Tampering | `app_settings.show_sample_data` default | mitigate | Default `show_sample_data` to false in schema and migration seed so sample data starts hidden from normal flows per D-04 and D-09. |
| T-01-03 | Elevation of Privilege | Single-admin/no-auth scope | accept | No auth is a locked milestone constraint; this plan does not add public access controls and later plans must surface deployment risk in README. |
| T-01-04 | Information Disclosure | Migration artifacts | mitigate | Migration files must contain only table/column names and default nonsecret settings, with no provider key values, ciphertext examples, IV examples, or auth-tag examples. |
| T-01-05 | Information Disclosure | Provider key leakage to browser | transfer | Plan 02 and Plan 05 enforce safe metadata DTOs; Plan 01 creates metadata types that exclude plaintext and encrypted material. |
| T-01-06 | Information Disclosure | Missing encryption secret unsafe fallback | transfer | Plan 02 owns fail-closed encryption behavior for `PROVIDER_KEYS_ENCRYPTION_SECRET`. |
| T-01-07 | Elevation of Privilege | Disabled key still used | transfer | Plan 03 owns resolver checks for `enabled === true`; Plan 01 provides the `enabled` column. |
| T-01-08 | Information Disclosure | Raw provider URL/key in validation errors | transfer | Plan 03 owns validation sanitization; Plan 01 does not call providers. |
| T-01-09 | Tampering | Sample source bypassing server-side gate | transfer | Plan 04 owns service-level sample gate enforcement; Plan 01 supplies the default hidden setting. |
</threat_model>

<verification>
Run focused tests for settings domain contracts, run `bun run typecheck`, run `bun run db:generate`, review generated SQL and journal metadata, and run `bun run db:migrate` only when `DATABASE_URL` is configured.
</verification>

<success_criteria>
Plan 01 is complete when settings defaults, provider descriptors, safe key metadata types, schema tables, and reviewed migration artifacts exist with no secret values in source or migration files.
</success_criteria>

<output>
After completion, create `.planning/phases/01-provider-settings-and-secrets/01-01-SUMMARY.md`.
</output>
